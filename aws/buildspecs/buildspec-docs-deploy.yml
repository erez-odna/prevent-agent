version: 0.2

phases:
  build:
    commands:
      - export OUT_FILE="docs.tar.gz"
      - export DOCSZIP=$(jq .DocsZip build.json -r)
      - aws s3 cp ${DOCSZIP} ./${OUT_FILE}
      - tar -xzvf ${OUT_FILE}
      - aws s3 sync ./docs/build s3://$DOCS_BUCKET/$ENV/$PROJECT_NAME/docs

